"""Splitting a dictionary into two lists"""

__author__ = "730439074"

def get_keys(input: dict[str,int]) -> list[str]:
    "Returns a list a lsit of the keys and id the input dict is empty then returns a empty list. "
    return [key for key in data]
for key in get_keys

def get_values(input: dict[str,int]) -> list[int]: 
    " Returns a list of all the values."
    return[value for value in data.values()]
